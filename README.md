# busybox-entware-example

An example showing how to keep docker images lightweight using 

- BusyBox official image as base(parent) image
- Installing packages using `opkg` and the Entware repository